spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/flowable?useUnicode=true&characterEncoding=utf8&&useSSL=false
    username: root
    password: root
logging:
  level:
    ROOT: debug

flowable:
  async-executor-activate: false
